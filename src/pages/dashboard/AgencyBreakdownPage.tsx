'use client'

import React from 'react'
import { PageHeader } from '@/design-system'

export default function AgencyBreakdownPage() {
  return (
    <div className="flex flex-col h-full">
      <PageHeader title="Agency Breakdown" />
      <div className="flex items-center justify-center flex-1 text-sendlytics-grey-400 text-base">
        Agency Breakdown — content pending
      </div>
    </div>
  )
}
